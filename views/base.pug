doctype html
html 
    head 
        block head
            meta(charset='UTF-8')
            meta(name='viewport' content='width=device-width, initial-scale=1.0')
            //interpolation syntax #{}
            //<link rel="stylesheet" href="css/style.css"</link>
            //note: we can use regular html also in pug.
            link(rel='stylesheet' href='/css/style.css')
            link(rel='shortcut icon' type='image/png' href='/img/favicon.png',)
            //remember we have already defined in the app.js file that all the static fiedls will be served from the public folder.
            //the line is [app.use(express.static(path.join(__dirname, 'public')));]
            //so all the above assests[style.css,favicon.png etc] are stored in the public folder.
            link(rel='stylesheet' href= 'https://fonts.googleapis.com/css?family=Lato:300,300i,700' )
            title Natours | #{title}
        
    body(data-alert =`${alert ? alert : ''}`) 
        // HEADER
        include _header

        // CONTENT
        block content
            h1 This is a place holder for heading
       
        //- // SECTION
        //- main.main
        //-     h1= tour

            
        // Footer
        include _footer

        //- script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/1.5.1/axios.min.js')
        //- script(type="module", src='/js/mapbox.js')
        //- script(type="module", src='/js/login.js')
        script(src='https://js.stripe.com/v3/')
        script( src='/js/bundle.js')
